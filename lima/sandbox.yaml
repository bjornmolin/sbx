# Lima VM template for the sbx sandbox runtime.
# A single shared VM, used by ALL sbx projects — created once, never recreated
# when adding projects. Per-project isolation lives one layer down, inside
# containerd (per-project network + proxy + cache volume + bind mount).
#
# On Linux the wrapper bypasses Lima entirely and uses rootless containerd
# directly; this template is macOS-only.

images:
- location: "https://cloud-images.ubuntu.com/releases/24.04/release/ubuntu-24.04-server-cloudimg-amd64.img"
  arch: "x86_64"
- location: "https://cloud-images.ubuntu.com/releases/24.04/release/ubuntu-24.04-server-cloudimg-arm64.img"
  arch: "aarch64"

vmType: "vz"

cpus: 4
memory: "8GiB"
disk: "60GiB"

# Broad mount: $HOME. Any project under $HOME can be sandboxed without a VM
# restart. Each project's CONTAINER still only sees its own directory via
# nerdctl bind mount — Lima sees more than the container does, deliberately.
mounts:
- location: "~"
  writable: true
mountType: "virtiofs"

containerd:
  system: false
  user: true

portForwards:
- guestPortRange: [1, 65535]
  ignore: true
